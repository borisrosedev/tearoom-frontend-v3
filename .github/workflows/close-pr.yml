name: Close-PR WF
run-name: ${{ github.actor }} is using the close-pr workflow 🚀
on:
  pull_request:
    types:
      - closed
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo The PR was merged
